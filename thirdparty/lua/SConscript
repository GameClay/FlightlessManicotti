import sys

Import('env')
coreEnv = env.Clone()


lua_lib = coreEnv.StaticLibrary('lua', 
	['etc/all.c'],
	CPPPATH = ['src'] + env['KL_PLATFORM_INC_PATH'],
	CCFLAGS = env['CCFLAGS'],
)
Return('lua_lib')